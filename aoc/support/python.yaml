test:
  name: test_day$day.py
  contents: |
    import typing

    import pytest

    from aoc.year$year.day$day.day$day import SAMPLE_INPUT, part_one, part_two

    T = typing.TypeVar("T")


    @pytest.mark.parametrize(
        ("expected", "func"),
        [
            (None, part_one),
            (None, part_two),
        ],
        ids=("one", "two"),
    )
    class TestYear$year:
        def test_day(self, expected: T, func: typing.Callable[[str], T]) -> None:
            assert expected == func(SAMPLE_INPUT)

run:
  name: day$day.py
  contents: |
    import aoclib

    SAMPLE_INPUT: str = """

    """

    def part_one(inputs: str) -> int:

        return

    def part_two(inputs: str) -> int:

        return
